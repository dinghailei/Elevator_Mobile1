<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a57617.elevator_mobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_TASKS" />

    <!-- 拍照 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.permission.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/schindler_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/schindler_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Register1Activity"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <activity android:name=".Forget1Activity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RobotActivity" />
        <activity android:name=".Robot1Activity" />
        <activity android:name=".PackageActivity" />
        <activity android:name=".TimeSettingActivity" />
        <activity android:name=".StoreySittingActivity" />
        <activity android:name=".PhotographActivity" />
        <activity android:name=".TakeShootingActivity"></activity>
    </application>

</manifest>